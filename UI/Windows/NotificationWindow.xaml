<Window x:Class="TriggerLAG.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" Height="Auto" Width="Auto"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Left="0" Top="0" SizeToContent="WidthAndHeight">
    
    <Grid>
        
        <Border x:Name="StyleMinimal" Visibility="Visible" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" 
                CornerRadius="2" Margin="0" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border.RenderTransform>
                <TranslateTransform x:Name="SlideTransformMinimal" X="300"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Direction="270" Color="Black" Opacity="0.5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Rectangle x:Name="StatusIndicatorMinimal" Grid.Column="0" Fill="#00FF00"/>
                
                <TextBlock x:Name="MessageTextMinimal" Grid.Column="1" Text="TRIGGERLAG ON" 
                           Foreground="#F0F0F0" FontWeight="SemiBold" FontSize="14" 
                           VerticalAlignment="Center" HorizontalAlignment="Center" 
                           FontFamily="Segoe UI" Margin="20,12,20,12"/>
            </Grid>
        </Border>

        
        <Border x:Name="StyleModern" Visibility="Collapsed" Background="#121212" BorderBrush="#444444" BorderThickness="1" 
                CornerRadius="16" Margin="0" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border.RenderTransform>
                <TranslateTransform x:Name="SlideTransformModern" X="300"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="4" Direction="270" Color="Black" Opacity="0.4"/>
            </Border.Effect>

            <Grid>
                <TextBlock x:Name="MessageTextModern" Text="TRIGGERLAG ON" 
                           Foreground="#FFFFFF" FontWeight="Bold" FontSize="13" 
                           VerticalAlignment="Center" HorizontalAlignment="Center" 
                           FontFamily="Segoe UI" Margin="24,10,24,10"/>
            </Grid>
        </Border>
    </Grid>
</Window>